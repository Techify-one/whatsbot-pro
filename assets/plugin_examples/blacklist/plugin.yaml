id: blacklist
name: Blacklist
version: 1.0.0
whatsbot_api_version: ">=1.0,<2.0"
description: Bloqueia mensagens recebidas de números listados — o bot nem salva nem chama o LLM. Demonstração de filter que aborta o pipeline retornando None.
author: WhatsBot
entry:
  filters: filters
  settings: settings
filters:
  - filter.message.before_save
permissions: []
dependencies: []

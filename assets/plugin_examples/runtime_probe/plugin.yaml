id: runtime_probe
name: Runtime Probe
version: 1.0.0
whatsbot_api_version: ">=1.0,<2.0"
description: Plugin de validação da fundação de runtime (plano 09). No setup registra um cleanup (on_unload) e uma task supervisionada "heartbeat" que faz broadcast de um tick periódico. Serve para validar lifecycle (setup/teardown aguardados) + supervisor de tasks ponta a ponta, sem subprocesso.
author: WhatsBot
entry:
  lifecycle: lifecycle
permissions:
  - runtime.task
dependencies: []

# Dados sensíveis / sessão WhatsApp / banco de dados
storages/
statics/

# Logs
logs/

# Ambiente virtual
venv/

# Python cache
__pycache__/
*.pyc
*.pyo

# Variáveis de ambiente
.env
.env.*

# Build artifacts
dist/
build/
*.exe
!bin/gowa.exe

# Plugins exportados (.zip distribuído pela Loja, não versionado no core)
*-plugin.zip

# Linux GOWA binary (baixado/buildado localmente por dev; não trackeado)
bin/gowa

# Docker persistent data
data/

# Plugin restart trigger (touched by plugins.restart at runtime)
server/_reload_trigger.py
.reload_sentinel

# Planos de implementação (local, não versionado)
PLANO_setup_wizard.md
.claude/projects
docs-planos/QA-motor-agentes.md
docs-planos/checklist-testar-tool-dependencias.md
docs-planos/QA-consolidado.md

# Migração de histórico Chatwoot (dumps + CSVs de PRODUÇÃO — nunca versionar)
plano-importacao-historico-chatwoot/

# Runbook de merge de contatos duplicados (nome civil + telefone de clientes REAIS,
# host do banco de produção — nunca versionar; o repositório é público)
plano-merge-contatos-duplicados/
